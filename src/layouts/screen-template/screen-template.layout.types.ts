/**
 * ScreenTemplateProps interface.
 * @interface ScreenTemplateProps
 * @since 1.0.0
 */
export interface ScreenTemplateProps {
  /** Children of the ScreenTemplate component. */
  children: React.ReactNode;
}
