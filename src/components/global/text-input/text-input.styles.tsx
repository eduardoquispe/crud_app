import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
    height: 72,
  },
  textError: {
    color: 'red',
    fontSize: 14,
  }
});

export default styles;