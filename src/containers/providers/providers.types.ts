import {ReactNode} from 'react';

/**
 * ProvidersProps interface.
 * @interface ProvidersProps
 * @since 1.0.0
 */
export interface ProvidersProps {
  /** Children of the Providers component. */
  children: ReactNode;
}
